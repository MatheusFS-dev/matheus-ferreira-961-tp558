During model training, the integrated multi-GNN architecture undergoes centralized optimization, allowing full access to
global CSI. The neural network’s weight and bias parameters
are adjusted through unsupervised learning, with parameter
updates guided by a loss function defined as